<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.dzp.clevergarlic.mapper.admin.SysLogMapper">

    <insert id="saveLog">
        insert into fc_sys_log
        (log_id,user_name,operation,time,method,params,ip,error_msg)
        values
        (#{logId},#{userName},#{operation},#{time},#{method},#{params},#{ip},#{errorMsg})
    </insert>

</mapper>